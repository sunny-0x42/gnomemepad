# Secrets & keys
.env
.env.*
!.env*.example
*.pem
*.key
.netlify/
web/.netlify/
server.log
server.err.log
web/server.log
web/server.err.log

# ── Local-only API helpers (not needed for Netlify CI) ──
# Read API for Netlify is published: web/lib/* + web/netlify/functions/*
web/server.mjs
# api-dev.mjs is tracked (local snapshot worker for markets speedups)
# Built frontend artifacts
web/.cache/
# Built frontend artifacts
web/ui/dist/
web/ui/node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.log

# Node
node_modules/
web/node_modules/

# Local chain / deploy artifacts (keep scripts, ignore generated outs if large)
deploy/topaz/**/gnomod.toml.bak

# Python
__pycache__/
*.pyc
.venv/
venv/

# Gno cache (if any under project)
.gno/

web/.cache/
